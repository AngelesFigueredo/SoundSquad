<h2>Post de {{post.author.username}}</h2>
<h2>{{post.title}}</h2>
<h3>{{post.content}}</h3>

{{#unless post.comments}}
<p>Este post no tiene comentarios</p>
{{/unless}}

{{#each post.comments}}
{{>commentPartial this}}
{{#if (ifEquals post.author._id ../user._id)}}
    <form action="comment/{{_id}}/delete" method="POST" id="delete-form">
      <button type="submit">🗑 comentario</button>
    </form>
  {{/if}}
{{/each}}